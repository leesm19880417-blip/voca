# 무거운 원본 PDF (업로드 금지 · 100MB 초과 + 저작권)
*.pdf

# 작업용/임시 폴더 (추출 원본·렌더 이미지)
_extract/
_pages/
_secret/
_lc/
_voca/
_ks/
_kst/
_ns/
_lg/
_more/
_tools/

# 로컬 설정
.claude/

# VOCA 음원 중 개별 단어(표제어별)는 제외 — 통합본만 사용
서아쌤의 토익 비밀과외 START_VOCA 음원/표제어별/

# 토익비밀과외 MP3 중 앱에서 안 쓰는 음원 제외 (Day _미 음원만 사용)
*DailyQuiz*.mp3
*실전TEST*.mp3

# OS 파일
Thumbs.db
.DS_Store
desktop.ini

# 개인 진도 백업 파일
toeic-voca-backup-*.json

# 1000문장 작업 폴더
_s1/
_s2/
_s3/

# 핵심패턴233 음원(대용량)·작업폴더 — GitHub Pages 1GB 한도 보호
_p233/
영어회화 핵심패턴 233/

# 100일 작업 폴더
_100/

# 책표지 원본 스크린샷(대용량) — 크롭본(covers/)만 사용
*.png
covers_test/

# 소통 영단어 작업 폴더
_inno/

# 경선식 최신 작업 폴더
_kst1/
_kst2/
_kst3/
_kstn/

# 웹푸시 VAPID 개인키(절대 커밋 금지) — GitHub 시크릿으로만 사용
_push_secret.txt

# 토익 모의고사 작업 폴더(렌더 이미지·OCR 파트)
_texam/

# AI 연상 생성 작업 폴더
_gentips/
_gentips2/

# LC 신규 음원 원본 zip(대용량)·작업 폴더 — 압축본(LC_bbo/LC_actual)만 사용
LC음원(new)/
_lcnew/

# AI LC 생성 작업 폴더
_lcgen/
_lcnew/

# 모의고사 원본·스크래치
서아쌤 모의고사/

# AI RC 생성 작업 폴더
_rcgen/

# 모의고사 RC OCR 작업 폴더
_rcx/

# 맨처음 LC 빌드 스크립트
_mclc_build.py

# 알림 정시설정 안내(개인용)
_알림_정시설정.txt

# ETS 음원 빌드 스크립트
_ets_audio.py
# ETS 원본(대용량)
ETS LC RC 기출문제/

# ETS Test1 OCR 작업
_ets1/
_ets_audio.py
